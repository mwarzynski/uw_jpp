test_for(i: int = +10): bool {
  j: int = 0;
  for j, false or j < i, j++ {
      print("o chuj, co tu sie odpierdala");
  }

  if true and i > j > r {
    arr: [bool, 10] = [ false, .. ];
    arr[0] = true;

    if arr[0] is false {
        return false;
    }
  }

  return true;
}
