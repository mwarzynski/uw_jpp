% Mateusz Warzyński, 371854

use_module(library(lists)).
% Also (np. member/2, append/3, length/2)

% jestWyborem(+AEgraf, -Graf)
% jestDFS(+Graf, -Lista)
% jestADFS(+AEgraf, -Lista)
% jestADFS1(+AEgraf, -Lista)

main :- write("Hello, World"),nl,fail.

